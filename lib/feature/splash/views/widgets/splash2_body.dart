import 'package:flutter/material.dart';

class Splash2Body extends StatelessWidget {
  const Splash2Body({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Center(child: Text('screen 2'))));
  }
}
